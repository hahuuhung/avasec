<UserControl x:Class="AVASec.UI.Views.CyberToolboxView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d" 
             d:DesignHeight="600" d:DesignWidth="800">
    <UserControl.Resources>
        <!-- Card Style / Kiểu Thẻ -->
        <Style x:Key="PluginCardStyle" TargetType="Border">
            <Setter Property="Background" Value="#1E293B"/>
            <Setter Property="CornerRadius" Value="12"/>
            <Setter Property="Margin" Value="0,0,16,16"/>
            <Setter Property="Padding" Value="16"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="BorderBrush" Value="#334155"/>
            <Setter Property="Effect">
                <Setter.Value>
                    <DropShadowEffect Color="Black" BlurRadius="10" Opacity="0.3" ShadowDepth="2"/>
                </Setter.Value>
            </Setter>
            <Style.Triggers>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="BorderBrush" Value="{StaticResource NeonCyanBrush}"/>
                    <Setter Property="Effect">
                        <Setter.Value>
                            <DropShadowEffect Color="#06B6D4" BlurRadius="15" Opacity="0.4" ShadowDepth="0"/>
                        </Setter.Value>
                    </Setter>
                </Trigger>
            </Style.Triggers>
        </Style>
    </UserControl.Resources>

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- Header -->
        <StackPanel Grid.Row="0" Margin="0,0,0,20">
            <TextBlock Text="Cyber-Toolbox" FontSize="24" FontWeight="Bold" Foreground="White" Margin="0,0,0,4"/>
            <TextBlock Text="Modular plugins to initiate specific tasks / Các plugin mô-đun để thực hiện tác vụ cụ thể" 
                       Foreground="#94A3B8" FontSize="14"/>
        </StackPanel>

        <!-- Tools Grid -->
        <ScrollViewer Grid.Row="1" VerticalScrollBarVisibility="Hidden">
            <ItemsControl x:Name="PluginsList">
                <ItemsControl.ItemsPanel>
                    <ItemsPanelTemplate>
                        <WrapPanel Orientation="Horizontal"/>
                    </ItemsPanelTemplate>
                </ItemsControl.ItemsPanel>
                <ItemsControl.ItemTemplate>
                    <DataTemplate>
                        <Border Style="{StaticResource PluginCardStyle}" Width="240" Height="140">
                            <Grid>
                                <Grid.RowDefinitions>
                                    <RowDefinition Height="Auto"/>
                                    <RowDefinition Height="*"/>
                                    <RowDefinition Height="Auto"/>
                                </Grid.RowDefinitions>
                                
                                <!-- Icon & Category -->
                                <Grid Grid.Row="0" Margin="0,0,0,10">
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="Auto"/>
                                        <ColumnDefinition Width="*"/>
                                    </Grid.ColumnDefinitions>
                                    <TextBlock Text="{Binding Metadata.IconUri}" FontSize="24" Margin="0,0,10,0"/>
                                    <Border Background="#334155" CornerRadius="4" Padding="6,2" HorizontalAlignment="Right">
                                        <TextBlock Text="{Binding Metadata.Category}" FontSize="10" Foreground="#E2E8F0"/>
                                    </Border>
                                </Grid>

                                <!-- Title -->
                                <TextBlock Grid.Row="1" Text="{Binding Metadata.Name}" FontSize="16" FontWeight="SemiBold" Foreground="White" TextWrapping="Wrap"/>
                                
                                <!-- Action Button -->
                                <Button Grid.Row="2" Content="Launch / Khởi chạy" 
                                        Click="LaunchPlugin_Click" Tag="{Binding Metadata.Id}"
                                        Style="{StaticResource PrimaryButtonStyle}"
                                        Background="Transparent" BorderBrush="{StaticResource NeonCyanBrush}" BorderThickness="1"
                                        HorizontalAlignment="Stretch"/>
                            </Grid>
                        </Border>
                    </DataTemplate>
                </ItemsControl.ItemTemplate>
            </ItemsControl>
        </ScrollViewer>

        <!-- Empty State -->
        <StackPanel x:Name="EmptyStatePanel" Grid.Row="1" VerticalAlignment="Center" HorizontalAlignment="Center" Visibility="Collapsed">
            <TextBlock Text="🧩" FontSize="48" HorizontalAlignment="Center" Margin="0,0,0,16" Foreground="#334155"/>
            <TextBlock Text="No plugins installed / Chưa cài đặt plugin nào" FontSize="16" Foreground="#64748B" HorizontalAlignment="Center"/>
            <Button Content="Refresh / Làm mới" Click="Refresh_Click" Margin="0,16,0,0" Style="{StaticResource PrimaryButtonStyle}" HorizontalAlignment="Center"/>
        </StackPanel>
    </Grid>
</UserControl>
