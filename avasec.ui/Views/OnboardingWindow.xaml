<Window x:Class="AVASec.UI.Views.OnboardingWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Chào mừng đến AVA Security / Welcome to AVA Security"
        Height="520" Width="640"
        WindowStartupLocation="CenterScreen"
        WindowStyle="None"
        AllowsTransparency="True"
        Background="Transparent"
        ResizeMode="NoResize">

    <Border CornerRadius="16" BorderBrush="#334155" BorderThickness="1">
        <Border.Background>
            <LinearGradientBrush StartPoint="0,0" EndPoint="1,1">
                <GradientStop Color="#0F172A" Offset="0"/>
                <GradientStop Color="#1E293B" Offset="1"/>
            </LinearGradientBrush>
        </Border.Background>

        <Grid Margin="32">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>

            <StackPanel Grid.Row="0" Orientation="Horizontal" HorizontalAlignment="Center" Margin="0,0,0,24">
                <TextBlock Text="🦉" FontSize="40" VerticalAlignment="Center"/>
                <StackPanel Margin="12,0,0,0">
                    <TextBlock Text="AVA Security" FontSize="28" FontWeight="Bold" Foreground="White"/>
                    <TextBlock Text="Faster. Safer. Smarter." FontSize="13" Foreground="#94A3B8"/>
                </StackPanel>
            </StackPanel>

            <StackPanel x:Name="StepPanel" Grid.Row="1" VerticalAlignment="Center">
                <TextBlock x:Name="StepTitle" Text="Bước 1: Quét thông minh" FontSize="22" FontWeight="SemiBold"
                           Foreground="White" HorizontalAlignment="Center" Margin="0,0,0,12"/>
                <TextBlock x:Name="StepDescription" TextWrapping="Wrap" TextAlignment="Center" FontSize="14"
                           Foreground="#CBD5E1" LineHeight="22" MaxWidth="480"/>
                <TextBlock x:Name="StepHint" TextWrapping="Wrap" TextAlignment="Center" FontSize="12"
                           Foreground="#64748B" Margin="0,16,0,0" MaxWidth="480"/>
            </StackPanel>

            <StackPanel Grid.Row="2" Orientation="Horizontal" HorizontalAlignment="Center" Margin="0,16,0,16">
                <Ellipse x:Name="Dot1" Width="10" Height="10" Fill="#06B6D4" Margin="4"/>
                <Ellipse x:Name="Dot2" Width="10" Height="10" Fill="#334155" Margin="4"/>
                <Ellipse x:Name="Dot3" Width="10" Height="10" Fill="#334155" Margin="4"/>
            </StackPanel>

            <Grid Grid.Row="3">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>
                <Button x:Name="SkipButton" Grid.Column="0" Content="Bỏ qua / Skip" Padding="16,8"
                        Background="Transparent" Foreground="#94A3B8" BorderThickness="0" Click="Skip_Click"/>
                <Button x:Name="BackButton" Grid.Column="1" Content="← Trước / Back" Padding="16,8" Margin="0,0,8,0"
                        Background="#334155" Foreground="White" BorderThickness="0" Click="Back_Click" Visibility="Collapsed"/>
                <Button x:Name="NextButton" Grid.Column="2" Content="Tiếp / Next →" Padding="20,8"
                        Background="#06B6D4" Foreground="White" BorderThickness="0" Click="Next_Click"/>
            </Grid>
        </Grid>
    </Border>
</Window>
