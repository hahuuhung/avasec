<Window x:Class="AVASec.UI.Views.NotificationWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        mc:Ignorable="d"
        Title="Thông báo / Notification" Height="350" Width="400"
        WindowStyle="None" AllowsTransparency="True" Background="Transparent"
        Topmost="True" ResizeMode="NoResize" ShowInTaskbar="False">
    
    <Border CornerRadius="12" Background="#0F172A" BorderBrush="#334155" BorderThickness="1">
        <Border.Effect>
            <DropShadowEffect BlurRadius="20" ShadowDepth="5" Opacity="0.5" Color="Black"/>
        </Border.Effect>
        
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/> <!-- Header -->
                <RowDefinition Height="Auto"/> <!-- Image -->
                <RowDefinition Height="*"/>    <!-- Content -->
                <RowDefinition Height="Auto"/> <!-- Actions -->
            </Grid.RowDefinitions>

            <!-- Header -->
            <Grid Grid.Row="0" Margin="16,12">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>
                <TextBlock Text="AVA Security System" Foreground="#94A3B8" FontSize="11" FontWeight="SemiBold"/>
                <Button Grid.Column="1" Content="✕" Width="20" Height="20" Background="Transparent" BorderThickness="0" Foreground="#64748B" Cursor="Hand" Click="CloseButton_Click"/>
            </Grid>

            <!-- Image (Optional) -->
            <Image x:Name="NotificationImage" Grid.Row="1" Height="140" Stretch="UniformToFill" Margin="0,0,0,12" Visibility="Collapsed"/>

            <!-- Content -->
            <StackPanel Grid.Row="2" Margin="20,0,20,10">
                <TextBlock x:Name="TitleText" Text="Notification Title" Foreground="White" FontSize="16" FontWeight="Bold" TextWrapping="Wrap" Margin="0,0,0,8"/>
                <TextBlock x:Name="MessageText" Text="This is a notification message body." Foreground="#CBD5E1" FontSize="13" TextWrapping="Wrap"/>
            </StackPanel>

            <!-- Actions -->
            <Grid Grid.Row="3" Margin="20,0,20,20">
                <Button x:Name="ActionButton" Content="Xem ngay / Details" Height="36" Background="#2563EB" Foreground="White" BorderThickness="0" Cursor="Hand" Click="ActionButton_Click" Visibility="Collapsed">
                    <Button.Resources>
                        <Style TargetType="Border">
                            <Setter Property="CornerRadius" Value="6"/>
                        </Style>
                    </Button.Resources>
                </Button>
            </Grid>
        </Grid>
    </Border>
</Window>
