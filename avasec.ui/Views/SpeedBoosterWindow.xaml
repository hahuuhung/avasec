<Window x:Class="AVASec.UI.Views.SpeedBoosterWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:AVASec.UI.Views"
        mc:Ignorable="d"
        Title="Speed Booster Ultra" Height="600" Width="900"
        WindowStartupLocation="CenterScreen"
        WindowStyle="None"
        AllowsTransparency="True"
        Background="Transparent">

    <Window.Resources>
        <!-- Cyber Colors -->
        <Color x:Key="NeonCyan">#06B6D4</Color>
        <Color x:Key="DeepBlue">#0F172A</Color>
        <Color x:Key="ElectricPurple">#8B5CF6</Color>
        <SolidColorBrush x:Key="NeonCyanBrush" Color="{StaticResource NeonCyan}"/>
        <SolidColorBrush x:Key="DeepBlueBrush" Color="{StaticResource DeepBlue}" Opacity="0.95"/>
        <SolidColorBrush x:Key="ElectricPurpleBrush" Color="{StaticResource ElectricPurple}"/>
        
        <!-- Glow Effect Style -->
        <Style x:Key="GlowText" TargetType="TextBlock">
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="Effect">
                <Setter.Value>
                    <DropShadowEffect Color="{StaticResource NeonCyan}" BlurRadius="10" ShadowDepth="0" Opacity="0.8"/>
                </Setter.Value>
            </Setter>
        </Style>

        <!-- Boost Button Style -->
        <Style x:Key="BoostButtonStyle" TargetType="Button">
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Grid>
                            <Ellipse x:Name="OuterRing" Stroke="{StaticResource NeonCyanBrush}" StrokeThickness="2" Width="180" Height="180" RenderTransformOrigin="0.5,0.5">
                                <Ellipse.Effect>
                                    <DropShadowEffect Color="{StaticResource NeonCyan}" BlurRadius="20" ShadowDepth="0"/>
                                </Ellipse.Effect>
                                <Ellipse.RenderTransform>
                                    <RotateTransform Angle="0"/>
                                </Ellipse.RenderTransform>
                            </Ellipse>
                            <Ellipse x:Name="InnerCircle" Fill="#1E293B" Width="150" Height="150"/>
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Grid>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="InnerCircle" Property="Fill" Value="#334155"/>
                            </Trigger>
                            <EventTrigger RoutedEvent="Button.Click">
                                <BeginStoryboard>
                                    <Storyboard>
                                        <DoubleAnimation Storyboard.TargetName="OuterRing" Storyboard.TargetProperty="(UIElement.RenderTransform).(RotateTransform.Angle)" From="0" To="360" Duration="0:0:0.5" RepeatBehavior="Forever"/>
                                    </Storyboard>
                                </BeginStoryboard>
                            </EventTrigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </Window.Resources>

    <Border Background="{StaticResource DeepBlueBrush}" CornerRadius="20" BorderBrush="{StaticResource NeonCyanBrush}" BorderThickness="1">
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="60"/> <!-- Header -->
                <RowDefinition Height="*"/>   <!-- Content -->
            </Grid.RowDefinitions>

            <!-- Background Grid Lines -->
            <Path Data="M0,0 L900,600 M900,0 L0,600" Stroke="#1E293B" StrokeThickness="1" Opacity="0.3" Grid.RowSpan="2"/>
            
            <!-- Header -->
            <Grid Grid.Row="0">
                <TextBlock Text="SPEED BOOSTER // ULTRA" FontSize="20" FontWeight="Bold" HorizontalAlignment="Left" VerticalAlignment="Center" Margin="24,0" Style="{StaticResource GlowText}"/>
                <Button Content="✕" HorizontalAlignment="Right" VerticalAlignment="Center" Margin="0,0,24,0" Click="CloseButton_Click" Background="Transparent" BorderThickness="0" Foreground="White" FontSize="20"/>
            </Grid>

            <!-- Content -->
            <Grid Grid.Row="1">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>

                <!-- Left: Metrics -->
                <StackPanel Grid.Column="0" VerticalAlignment="Center" HorizontalAlignment="Center">
                    <TextBlock Text="CPU LOAD" Foreground="#94A3B8" FontSize="14" HorizontalAlignment="Center" Margin="0,0,0,10"/>
                    <ProgressBar Width="120" Height="10" Value="45" Background="#1E293B" Foreground="{StaticResource NeonCyanBrush}" BorderThickness="0"/>
                     <TextBlock x:Name="CpuText" Text="45%" FontSize="32" FontWeight="Bold" Foreground="White" HorizontalAlignment="Center" Style="{StaticResource GlowText}" Margin="0,10,0,0"/>
                    
                    <TextBlock Text="RAM USAGE" Foreground="#94A3B8" FontSize="14" HorizontalAlignment="Center" Margin="0,30,0,10"/>
                    <ProgressBar Width="120" Height="10" Value="62" Background="#1E293B" Foreground="{StaticResource ElectricPurpleBrush}" BorderThickness="0"/>
                    <TextBlock x:Name="RamText" Text="62%" FontSize="32" FontWeight="Bold" Foreground="White" HorizontalAlignment="Center" Style="{StaticResource GlowText}" Margin="0,10,0,0"/>
                </StackPanel>

                <!-- Center: Boost Button -->
                <Grid Grid.Column="1" VerticalAlignment="Center" HorizontalAlignment="Center">
                    <Button x:Name="BoostBtn" Style="{StaticResource BoostButtonStyle}" Click="BoostButton_Click">
                        <StackPanel>
                            <TextBlock Text="BOOST" FontSize="24" FontWeight="Bold" Foreground="White" HorizontalAlignment="Center">
                                <TextBlock.Effect>
                                    <DropShadowEffect Color="White" BlurRadius="10" ShadowDepth="0"/>
                                </TextBlock.Effect>
                            </TextBlock>
                            <TextBlock Text="NOW" FontSize="12" Foreground="{StaticResource NeonCyanBrush}" HorizontalAlignment="Center"/>
                        </StackPanel>
                    </Button>
                </Grid>

                <!-- Right: Process List -->
                <Border Grid.Column="2" Background="#1E293B" CornerRadius="12" Margin="20" Padding="16" BorderBrush="#334155" BorderThickness="1">
                    <Grid>
                        <Grid.RowDefinitions>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="*"/>
                        </Grid.RowDefinitions>
                        <TextBlock Text="BACKGROUND APPS" Foreground="#94A3B8" FontSize="12" FontWeight="Bold" Margin="0,0,0,12"/>
                        <ListBox x:Name="ProcessList" Grid.Row="1" Background="Transparent" BorderThickness="0" Foreground="White">
                            <ListBox.ItemTemplate>
                                <DataTemplate>
                                    <Grid Margin="0,4">
                                        <Grid.ColumnDefinitions>
                                            <ColumnDefinition Width="*"/>
                                            <ColumnDefinition Width="Auto"/>
                                        </Grid.ColumnDefinitions>
                                        <TextBlock Text="{Binding Name}" Foreground="White"/>
                                        <TextBlock Grid.Column="1" Text="{Binding MemoryUsage}" Foreground="#EF4444" FontSize="11"/>
                                    </Grid>
                                </DataTemplate>
                            </ListBox.ItemTemplate>
                        </ListBox>
                    </Grid>
                </Border>
            </Grid>
        </Grid>
    </Border>
</Window>
