<UserControl x:Class="AVASec.UI.Controls.NotificationToast"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             Width="350" Height="100">
             
    <UserControl.Resources>
        <Storyboard x:Key="SlideIn">
            <DoubleAnimation Storyboard.TargetProperty="Opacity" From="0" To="1" Duration="0:0:0.3"/>
            <ThicknessAnimation Storyboard.TargetProperty="Margin" From="0,20,0,-20" To="0,0,0,10" Duration="0:0:0.3"/>
        </Storyboard>
        <Storyboard x:Key="FadeOut">
             <DoubleAnimation Storyboard.TargetProperty="Opacity" From="1" To="0" Duration="0:0:0.5" BeginTime="0:0:4"/>
        </Storyboard>
    </UserControl.Resources>

    <Border Background="#1E293B" CornerRadius="12" BorderBrush="#334155" BorderThickness="1" Margin="5">
        <Border.Effect>
            <DropShadowEffect Color="Black" BlurRadius="15" ShadowDepth="5" Opacity="0.5"/>
        </Border.Effect>
        <Grid>
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="60"/>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="30"/>
            </Grid.ColumnDefinitions>
            
            <!-- Icon -->
            <Border x:Name="IconBorder" Background="#0F172A" CornerRadius="10" Width="40" Height="40" HorizontalAlignment="Center" VerticalAlignment="Center">
                 <TextBlock x:Name="IconText" Text="🔔" FontSize="20" HorizontalAlignment="Center" VerticalAlignment="Center" Foreground="White"/>
            </Border>
            
            <!-- Text -->
            <StackPanel Grid.Column="1" VerticalAlignment="Center" Margin="5,0">
                <TextBlock x:Name="TitleText" Text="Notification Title" FontWeight="Bold" Foreground="White" FontSize="14"/>
                <TextBlock x:Name="MessageText" Text="This is a sample notification message." Foreground="#94A3B8" FontSize="12" TextWrapping="Wrap"/>
            </StackPanel>
            
            <!-- Close -->
            <Button Grid.Column="2" Content="✕" Click="Close_Click" Background="Transparent" BorderThickness="0" Foreground="#64748B" VerticalAlignment="Top" HorizontalAlignment="Right" Margin="0,5,5,0"/>
        </Grid>
    </Border>
</UserControl>
