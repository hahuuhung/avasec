<Window x:Class="AVASec.Plugins.PrivacyShredder.ShredderWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Privacy Shredder" Height="450" Width="600"
        WindowStartupLocation="CenterScreen"
        Background="#0F172A" Foreground="White">
    <Window.Resources>
        <Style TargetType="Button">
            <Setter Property="Background" Value="#EF4444"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="Padding" Value="10,5"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}" CornerRadius="4">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </Window.Resources>

    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Header -->
        <StackPanel Grid.Row="0" Margin="0,0,0,20">
            <TextBlock Text="🗑️ Privacy Shredder" FontSize="24" FontWeight="Bold" Foreground="#EF4444"/>
            <TextBlock Text="Files deleted here cannot be recovered. Use with caution." Foreground="#94A3B8"/>
        </StackPanel>

        <!-- File List -->
        <Border Grid.Row="1" Background="#1E293B" CornerRadius="8" Padding="10">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="*"/>
                </Grid.RowDefinitions>
                <TextBlock Text="Selected Files / Tệp đã chọn:" Foreground="#94A3B8" Margin="0,0,0,10"/>
                <ListBox Grid.Row="1" x:Name="FileListBox" Background="Transparent" BorderThickness="0" Foreground="White" FontSize="14"/>
            </Grid>
        </Border>

        <!-- Actions -->
        <StackPanel Grid.Row="2" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,20,0,0">
            <Button Content="Add File..." Click="AddFile_Click" Background="#334155" Margin="0,0,10,0"/>
            <Button Content="SHRED NOW / XÓA VĨNH VIỄN" Click="Shred_Click" FontWeight="Bold"/>
        </StackPanel>
    </Grid>
</Window>
